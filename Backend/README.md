# Etablissement Full Backend (PostgreSQL)

Edit src/main/resources/application.properties and set your Postgres credentials.
Create DB: CREATE DATABASE etablissementdb;

Build: mvn clean install
Run: mvn spring-boot:run

